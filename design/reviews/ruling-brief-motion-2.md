# Motion review 2 — The Ruling Brief (ChatGPT temporary chat, High, "worked for 5m 38s")

2026-09-06 ~16:05 Sydney (+10:00). Inputs: the 20 ms queue sequence (four images), native 1:1 crops of the left column (numeral, question, docket, tray) and right column (pills, pressed pill, thread origin) at pre-press / 0 ms / first rAF / 20 ms, native hold left and right, and a `prefers-reduced-motion: reduce` pre/post pair. Context given: round 1's findings, the implementer's reading of item 1 (curve canonical, storyboard note corrected, owner told), and the docket fix (docket grows through the same 140 ms transition the body shrinks with).

## VERDICT: ASK OWNER (verification pass pending true 1:1 pixels — see 7)

1. **Storyboard intent now matches.** Idle row → press → thread grows toward the fixed tray → tray count changes → body collapses into the teal docket → following rows move up and renumber. The earlier ~36 px 0 ms downward bump is gone; the acted row only loses height, so the rows beneath never take the wrong-direction excursion.
2. **Cadence is consistent with the canonical easings; do not change them.** `220ms cubic-bezier(.16,1,.3,1)`: ≈40% at 16.7 ms, 46% at 20, 85% at 60, 92% at 80, 98% at 120, 100% at 220 — the thread reads as an early, decisive departure with a long settle. `140ms cubic-bezier(.4,0,.2,1)`: ≈2.9% at 16.7, 5.9% at 20, 32.8% at 40, 66.8% at 60, 85.2% at 80, 94.5% at 100, 98.8% at 120, 100% at 140 — the row/next-row displacement follows that slower-starting profile. Keep both unchanged.
3. **Start synchronisation looks correct.** No developed thread and no developed docket in the pre and 0 ms captures; by the first rAF at 16.7 ms both have begun and both are clearer at 20 ms. No one-frame docket/thread sequencing error; the answer pill reads as the common origin of both consequences rather than two serial effects.
4. **Renumbering is at motion start, not at collapse completion.** The numeral state has changed in the 0 ms capture while geometry is still at its start; the 16.7/20 ms captures show the rows beginning their upward travel. Intended ordering — do not delay numeral replacement to 140 ms.
5. **The fixed-viewport 0–220 ms sequence plus hold is sufficient to judge macro motion.** No bounce, overshoot, reversal, late snap, or generic fade/slide substituted for the specified behaviour. The thread remains the authored directional cue; the tail is appropriately dominated by the thread finishing rather than continued layout movement.
6. **Reduced motion is correct.** The pre/post pair shows the semantic state change with no interpolated intermediate state and the thread omitted. Keep 0 ms state changes and no thread under `prefers-reduced-motion`; do not retain the 140 ms collapse or the 220 ms thread there.
7. **Remaining blocker is evidentiary, not a motion-design change.** Every raster delivered — including images 5–8 labelled native — arrived at 314 × 118 px, which prevents a defensible confirmation of the 1 px thread thickness, the `scaleX(.997)` contraction, and the exact cubic control points from pixel measurement. Re-send images 5–8 as unscaled PNG crops from the 1536 × 874 viewport. Keep the build values exactly `1px`, `scaleX(.997)` with the right edge as transform origin, `220ms cubic-bezier(.16,1,.3,1)`, `140ms cubic-bezier(.4,0,.2,1)`. With true 1:1 pixels this should be a verification pass rather than a redesign round.

## Implementer's reading

Item 7 is a transport defect on this side: the Mac-Chrome screenshots used for the upload were taken at 0.2 scale, so the "native" crops reached the reviewer as thumbnails. Round 3 re-sends the same four native crops at 1:1 (the served PNGs are 1568 px wide, the Chrome viewport is 1568 × 592, so a full-scale screenshot is pixel-exact except for JPEG transport). No build change between rounds 2 and 3.
